# 1. 复制本文件为 .env（与 hardhat.config.js 同目录）
# 2. 把下面两行改成你的真实 RPC 与私钥；Hardhat 会通过 dotenv 自动加载 .env
# 3. 勿将 .env 提交到 Git
#
# ----- SEPOLIA_RPC_URL 常见错误（会报 HHE8 Invalid URL）-----
# MetaMask「网络设置」里只显示 sepolia.infura.io，那是简称，不能直接贴进 Hardhat。
# 必须是「完整 URL」：
#   - 以 https:// 开头
#   - Infura 形如：https://sepolia.infura.io/v3/<你在 Infura 控制台项目里复制的 API Key>
# 请到 https://infura.io 登录 → 打开项目 → 选 Sepolia → 复制「HTTPS」端点整串粘贴到这里。
# 错误示例：sepolia.infura.io、https://sepolia.infura.io（缺少 /v3/密钥）
#
# 亦可不设 .env，在 PowerShell 中临时导出：
#   $env:SEPOLIA_RPC_URL="https://..."
#   $env:SEPOLIA_PRIVATE_KEY="0x..."
#
# 或使用 keystore：npx hardhat keystore set SEPOLIA_RPC_URL

# 必须是 Sepolia，不能填 mainnet.infura.io。整串从控制台复制，例如：
# https://sepolia.infura.io/v3/xxxxxxxx
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY
# 必须是「账户私钥」：64 位十六进制（+ 可选 0x），共 32 字节。不要填钱包地址。
SEPOLIA_PRIVATE_KEY=0x你的部署私钥
